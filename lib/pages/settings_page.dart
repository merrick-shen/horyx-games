import 'package:flutter/material.dart';

/// 设置页（占位）
/// 当前为空白页面，设置功能在后续迭代中补充
class SettingsPage extends StatelessWidget {
  const SettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    // 空白占位：仅保证背景色与应用主题一致
    return const Scaffold();
  }
}
